name: Test

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  prepublish:
    runs-on: ubuntu-latest
    steps:
      - name: Setup
        uses: ./.github/actions/setup
      - name: Pre-publish
        run: npm run vscode:prepublish
